<section class="grid w-full sm:w-3/4 md:max-w-md lg:max-w-lg rounded-xl border-4 border-primary-500 dark:border-primary-700 bg-secondary-50 dark:bg-transparent p-5 md:p-8 lg:px-12">
  <%# New birthday_buddy form %>
  <%= render "form", buddy: @birthday_buddy %>
</section>

<%# divider %>
<hr class="divider">

<%# Birthdays %>
<% if @birthday_buddies.present? %>
  <article data-controller="dialog" class= "w-full sm:max-w-md lg:max-w-lg grid justify-items-stretch gap-8 md:gap-14 mb-16 md:mb-10">
    <h2 class="font-semibold text-2xl md:text-3xl tracking-wide text-center text-primary-950 dark:text-primary-100">🎂&nbsp;&nbsp;Upcoming birthdays! &nbsp;&nbsp;🎉</h2>

    <%= turbo_stream_from current_user %>
    <div id="birthday_buddies" class="grid gap-8 md:gap-12">
      <%= render @birthday_buddies %>
    </div>
  </article>
<% end %>